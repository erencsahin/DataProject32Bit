spring.application.name=coordinator
server.port=1502

spring.data.redis.host=${SPRING_REDIS_HOST:redis}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}

spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:kafka:9093}
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

spring.kafka.consumer.group-id=avg-consumers
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages="*"

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/toyotadb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:toyota}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:secret}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# OpenSearch için
opensearch.host=${OPENSEARCH_HOST:opensearch}
opensearch.port=${OPENSEARCH_PORT:9200}

#SMTP için
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=erencsahin0@gmail.com
spring.mail.password=twsn qotz rluw ezqt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.to=erencsahin34@gmail.com
app.mail.from=erencsahin0@gmail.com